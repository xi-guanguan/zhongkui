# 多人模式云变量安全写入规范

| 字段 | 值 |
|------|-----|
| 这是什么 | 多人游戏中 clientCloud/serverCloud 的可用性规则和防作弊写入规范——覆盖多人模式下 clientCloud 为 nil 的问题、服务端中转读写模式、敏感数值服务端直写、数据流架构图、新增云变量检查清单 |
| 原作者 | 猎猫人 |
| 推荐度 | L3（经验有效但缺少通用验证，部分内容是项目特定的字段名） |
| 适用场景 | 多人游戏中使用 serverCloud 做数据持久化和防作弊 |

---

## 注意事项 1：多人模式下 clientCloud 不可用

| 模式 | clientCloud | serverCloud | 解决方案 |
|------|------------|------------|---------|
| 单机 | 可用 | 不可用 | 直接使用 clientCloud |
| 多人客户端 | **不可用（nil）** | 不可用 | 通过远程事件请求服务端代为操作 |
| 多人服务端 | 不可用 | 可用 | 直接使用 serverCloud |

### 数据读取：服务端推送替代客户端拉取

```
❌ 错误（多人模式会失败）：
  客户端 clientCloud:BatchGet → 读取数据

✅ 正确：
  客户端连接
    → 服务端 HandleClientReady
    → serverCloud:BatchGet(userId) 读取数据
    → SendRemoteEvent 推送给客户端
    → 客户端 onServerInit() 初始化本地显示
```

### 数据写入：远程事件中转替代客户端直写

```
❌ 错误（多人模式会失败）：
  客户端 clientCloud:BatchSet → 写入数据

✅ 正确：
  客户端 SendRemoteEvent(PROGRESSION_SAVE, data)
    → 服务端 HandleProgressionSave
    → serverCloud:Get 读取 → 合并 → serverCloud:Set 写回
```

排行榜同理，通过服务端中转。

---

## 注意事项 2：防作弊——敏感数值必须服务端写入

可被玩家利益驱动篡改的数值，必须由服务端写入：

| 写入方式 | API | 安全性 | 适用场景 |
|---------|-----|--------|---------|
| 服务端直接写入 | serverCloud | 安全，服务端权威 | 经验、积分、货币、排行榜数值 |
| 客户端中转远程事件 → 服务端 | 远程事件 | 服务端可校验上限 | 非核心但需持久化的数据 |

### 安全分级示例

**高安全（服务端直接写入）**：

| 数据 | 写入方式 | 说明 |
|------|---------|------|
| 总游玩时长 | `serverCloud:Add(uid, key, 60)` | 每 60 秒原子累加 |
| 每日游玩经验 | `serverCloud:Set(uid, key, daily)` | 服务端读取→递增→写回 |

**中安全（客户端中转 + 服务端校验）**：

| 数据 | 写入方式 | 说明 |
|------|---------|------|
| 通行证经验 | 远程事件 → HandleProgressionSave | 服务端做上限校验 |
| 无尽积分 | 远程事件 → HandleProgressionSave | 服务端做上限校验 |
| 熟练度 | 远程事件 → HandleProgressionSave | 服务端做上限校验 |

> 注意：中安全级别的数据由客户端提交数值，服务端仅做上限校验。如果涉及奖励兑换，应改为服务端直接计算。

---

## 数据流架构

```
┌─────────────────┐                                ┌─────────────────┐
│   Server.lua    │                                │   Client.lua    │
│                 │   PROGRESSION_SYNC(初始)         │                 │
│  连接时:         │  ──────────────────────────▶   │  转发给:         │
│  BatchGet(uid)  │   全量数据推送                   │  Standalone     │
│  → 推送初始数据  │                                │  .OnProgInit()  │
│                 │                                │                 │
│  每60秒:         │   PROGRESSION_SYNC(增量)        │                 │
│  serverCloud    │  ──────────────────────────▶   │  .OnProgSync()  │
│  :Add(+60s)     │   PlayTimeDelta / DailyPlayExp │                 │
│  :Get→+1exp     │                                │                 │
│  :Set(daily)    │   PROGRESSION_SAVE             │  通关/失败时:    │
│                 │  ◀──────────────────────────   │  saveToCloud()  │
│  HandleProgSave │   PassExp/EndlessScore/Mastery │  → 远程事件      │
│  :Get→合并→:Set │                                │                 │
└─────────────────┘                                └─────────────────┘
```

---

## 新增云变量检查清单

添加新的云变量前，回答以下问题：

- [ ] 多人模式下谁来读写？→ clientCloud 不可用，必须走服务端
- [ ] 该数值是否影响玩家收益（经验、货币、排名）？→ 服务端直接写入
- [ ] 该数值是否可以被客户端伪造来获得不当利益？→ 服务端直接写入
- [ ] 该数值是否仅影响 UI 展示、无利益关联？→ 可由客户端中转服务端写入
- [ ] 是否有每日上限？→ 服务端校验上限，不要只依赖客户端检查
- [ ] 读取时机？→ 在 HandleClientReady 中 BatchGet 并推送给客户端

---

## 相关文件（参考项目结构）

| 文件 | 职责 |
|------|------|
| `scripts/network/Server.lua` | 服务端追踪逻辑、初始数据推送、保存处理 |
| `scripts/network/Client.lua` | 订阅 PROGRESSION_SYNC 事件 |
| `scripts/network/Shared.lua` | 事件名定义 |
| `scripts/network/Standalone.lua` | 单机模式转发 |
| `scripts/progression/data.lua` | 数据初始化、增量更新、saveToCloud 中转 |
